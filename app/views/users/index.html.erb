<div>
  <div>
    <h1>
      List of all users
    </h1>
  </div>
</div>

<hr>
<div>
  <div>
    <table>
      <tr>

        <th>
          Name
        </th>

        <th>
          Photo
        </th>

        <th>
          User
        </th>

        <th>
          Friend
        </th>

        <th>
        </th>
      </tr>

      <% @list_of_users.each do |a_user| %>
      <tr>
        <td>
          <%= a_user.first_name + " " + a_user.last_name%>
        </td>

        <td>
          <%= a_user.photo%>
        </td>

        <td>
          <a href="/users/<%= a_user.id %>">
            Show details
          </a>
        </td>


        <td>
          <% the_friend = @current_user.friends.where({ :course_id => a_course.id }).at(0) %>

          <% if the_like == nil %>
          <form action="/insert_like" method = "post">

            <input type="hidden" name="query_course_id" value="<%= a_course.id %>">
            <button>Like this course!</button>

          </form>

          <% else %>
            <a href="/delete_like/<%= the_like.id %>">
              Remove like
            </a>
          <% end %>
        </td>






      </tr>
      <% end %>
    </table>
  </div>
</div>

<hr>
